apply plugin: 'java-library'

dependencies {
    implementation fileTree(dir: 'libs', include: ['*.jar'])

    compile 'com.android.support:support-annotations:26.1.0@jar'
}

sourceCompatibility = "1.7"
targetCompatibility = "1.7"
